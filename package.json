{
  "name": "inventory-system",
  "version": "1.0.0",
  "description": "库存管理系统 - Cloudflare Workers + D1 + R2",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create inventory_db",
    "db:migrate": "wrangler d1 execute inventory_db --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^3.12.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "wrangler": "^3.22.0"
  }
}
